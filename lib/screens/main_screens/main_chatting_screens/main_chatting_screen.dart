import 'package:flutter/material.dart';

class MainChattingScreen extends StatelessWidget {
  const MainChattingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('서비스 준비중입니다.'),
    );
  }
}
